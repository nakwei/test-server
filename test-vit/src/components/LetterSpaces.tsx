// thinking flex box of lines.
// component would take in word.length prop.

export const LetterSpaces: FC <{letters: number}> = (letters) => {
    return (
        <div className="flex space-x-4">
            {
                //for loop? and create a line?
            }
            
        </div>
        
    )
}